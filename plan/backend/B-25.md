# B-25 — Audit trail

| | |
|---|---|
| **Fase** | 5: Documents & Reports |
| **Type** | Backend |
| **Dependencies** | B-06 |
| **Status** | ✅ Selesai |

---

## Deskripsi
Log all important actions.

## Detail Teknis
`app/services/audit.py`.
Function `log_action(user_id, action, entity_type, entity_id, detail)`.
Call this from all important endpoints (create payment, create bill, void payment, etc.).
GET `/audit-logs` — list logs, filter by date range, user_id, action type. Pagination. Admin-only. Read-only (no delete/edit endpoints).

## Acceptance Criteria
- [x] All important actions logged
- [x] Filter works
- [x] Cannot delete logs
